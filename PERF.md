# Performance Round

We need to improve overall performance of the system.

Issues when we start (very slow)
Issues with recovery and backup and failover (very slow)
Issues basically everywhere with performance (very slow)


Example:

The workspace pass is down to the CapDB restart test again, which is the slowest piece but has been passing cleanly after our earlier rerun. I’m letting it finish rather than interrupting it so we keep one authoritative full-suite result.


