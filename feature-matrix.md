# CapDB Feature Matrix

Fourth and final deep-dive basis: live source review across `core/`, `capdb/`, `bindings/`, `extensions/`, `tools/`, `tests/`, docs, CI, release scripts, install output, pkg-config output, man pages, and binding shims on 2026-07-08.

Verification performed during this final review:

- `CAPDB_WERROR=ON ./tools/warnings.sh` passed for CapDB-owned warning targets, including `capdb_client`, `capdb-server`, `capdbtest`, `capdb_nettest`, `capsuite`, `threadtest_pool`, and `capdb_jni`.
- `python3 tools/py/tests/test_codegen_parity.py` passed: 13 tests.
- Release/CI audit confirmed `.github/workflows/ci.yml` exists, but no `.github/workflows/release.yml` exists despite `scripts/init-standalone-repo.sh` saying tag pushes trigger it.

Verification carried forward from prior passes in the same working tree:

- `cmake --build build -j$(nproc)` passed.
- `ctest --output-on-failure` passed: 31/31 tests.
- `cmake --install build --prefix /tmp/capdb-install-review` completed.
- Installed CLI smoke passed: `capdb -batch ... 'SELECT 1;'`.
- Installed file audit confirmed the install set includes `capdb`, `capdb-server`, `libcapdb`, `libcapdb_client`, `capdb.h`, `capdb_client.h`, `capdb_pool.h`, man pages, helper scripts, and `libcapdb_client.pc`.
- Installed pkg-config audit found `libcapdb_client.pc` exists but still emits the configure-time `/usr/local` prefix when installing with a different `--prefix`; no `capdb.pc` is installed.
- Go binding tests passed with `go test -tags capdb -v ./capdb`.
- Rust binding tests passed with `cargo test`.
- Python embedded smoke passed when `PYTHONPATH`, `CAPDB_LIBRARY`, and `LD_LIBRARY_PATH` were set explicitly.
- Packaging fix pass: `cmake -S . -B build`, targeted rebuild of install artifacts, and `cmake --install build --prefix /tmp/capdb-install-row20` passed after install-rule updates. The temp install now includes `capdb-ctl`, `capdb.pc`, store/cluster/replication public headers, and prefix-correct `pkg-config` output for `capdb` and `libcapdb_client`.
- Replica read-routing fix pass: rebuilt `capdb_client`, `capdb_nettest`, and `capsuite`; `ctest --test-dir build --output-on-failure -R 'capdb_net|capdb_loopback|capdbtest'` passed 28/28 after prepared statements learned to retain their selected stream. Added and passed `capdb_net_capdb_28_read_preference`, which connects with `read_preference=replica`, stops the primary, then verifies `EXEC` and prepared `WITH ... SELECT` reads still succeed through the replica stream.
- Final implementation verification: `cmake -S . -B build`, `cmake --build build -j$(nproc)`, full `ctest --test-dir build --output-on-failure` passed 32/32, `CAPDB_WERROR=ON ./tools/warnings.sh` passed, `python3 tools/py/tests/test_codegen_parity.py` passed, Go binding tests passed, Rust binding tests passed, Python binding smoke passed, and a temp-prefix install audit confirmed current binaries, headers, man pages, systemd template, and pkg-config files.
- First-ten-row completion pass: added API-surface audit, extension smoke, CI install smoke, SHA-256 auth-file secrets, remote VFS delete RPC, guarded server-side VFS unlink, and VFS delete regression. Verification passed: `cmake -S . -B build`, `cmake --build build -j$(nproc)`, full `ctest --test-dir build --output-on-failure` passed 33/33, `CAPDB_WERROR=ON ./tools/warnings.sh` passed, `python3 tools/py/tests/test_codegen_parity.py` passed, `./tools/check-api-surface.sh` passed, and temp-prefix install/pkg-config/CLI smoke passed.
- Second-ten-row completion pass: persisted store role state, added `capdb-ctl demote`, made prepared-volume snapshots/export paths work before `data/main.db` exists, resumed replicas from local applied LSN, implemented multi-replica read endpoint fan-out, added Java wrapper compile/install coverage, and added versioned binding-source release packaging. Verification passed: `cmake --build build -j$(nproc)`, focused `ctest --test-dir build --output-on-failure -R 'store_test|java_jni_wrapper_compile|capdb_net_capdb_28_read_preference|extension_smoke'` passed 4/4, full `ctest --test-dir build --output-on-failure` passed 34/34, `CAPDB_WERROR=ON ./tools/warnings.sh` passed, `python3 tools/py/tests/test_codegen_parity.py` passed, `./tools/check-api-surface.sh` passed, and temp-prefix install/pkg-config/CLI/JNI-wrapper smoke passed.
- Final non-100-row completion pass: added JNI runtime load coverage, install-tree smoke, release artifact smoke/audit, CapDB-native upstream-archive/fuzz/soak smoke, upstream-sync automation, compatibility ledger, JSON health/status, Prometheus-style metrics, backup restore drill, and operations smoke. Verification passed: `cmake -S . -B build`, `cmake --build build -j$(nproc)`, full `ctest --test-dir build --output-on-failure` passed 41/41, `CAPDB_WERROR=ON ./tools/warnings.sh` passed, `python3 tools/py/tests/test_codegen_parity.py` passed, and `./tools/check-api-surface.sh` passed.

Percentages are product-readiness estimates, not line-count estimates. A feature can be implemented and test-green but still partial when the repo shows missing install coverage, docs drift, failover gaps, packaging friction, or narrow test depth.

| Order in which to complete the feature | State | Percentage complete | Description | Dependancies |
|---:|---|---:|---|---|
| 1 | complete | 100% | Core SQL engine fork and CapDB rebrand. The SQLite-derived engine is reorganized under `core/`, generated as `capdb.c`/`capdb.h`, exposes `capdb_*`/`CAPDB_*`, and removes old `sqlite3`/`msqlite://` compatibility shims from the primary API surface. `tools/check-api-surface.sh` now CI-checks public headers against legacy API names while preserving upstream/reference docs where appropriate. | SQLite 3.54 lineage, Python codegen, CMake, rebrand tooling, API-surface audit. |
| 2 | complete | 100% | CMake build and generated-source pipeline. CMake drives header generation, Lemon parser generation, opcode/keyword generation, staged source collection, amalgamation generation, CLI/server/client/store/shared-library builds, codegen dependency ordering, warning builds, CPack setup, CTest registration, install smoke, extension smoke, binding smoke, and release workflow packaging without Tcl. | CMake 3.16+, Python 3, Lemon, OpenSSL for network, zlib when enabled, CI smoke checks. |
| 3 | complete | 100% | Local `capdb` CLI and embedded library mode. The CLI builds from generated shell sources, supports local databases and remote `capdb://` routing, and installed CLI smoke is now enforced in CI after `cmake --install`. `libcapdb`/`libcapdb.so`, `capdb.pc`, headers, man pages, and packaged helper artifacts are installed and pkg-config checked. | Core engine, generated shell, generated public headers, install rules, CI install smoke. |
| 4 | complete | 100% | Built-in and loadable SQL extension surface. The supported CapDB extension surface is explicitly scoped to CMake-backed FTS5, math functions, percentile, and shell-integrated expert/intck/recover helpers; retained upstream trees are documented as reference/experimental unless productized. `extension_smoke` now exercises FTS5, math, and percentile through the built CLI. | Core extension hooks, CMake options, generated FTS5 sources, extension smoke test, scoped extension docs. |
| 5 | complete | 100% | Connection pool API. `capdb_pool_open/acquire/release/close/stats` are implemented with private page caches, WAL defaults, busy timeouts, serialized write checkout, reset-on-release, close-busy handling, concurrency tests, correct public docs, installed headers, and CI package/install smoke coverage. | Core engine, pthread mutexes/conditions, WAL mode, active pool tests, install smoke. |
| 6 | complete | 100% | TLS/network transport and binary frame protocol. The protocol has versioned `HELLO`, auth, open, exec, prepare/step/finalize, close, ping/pong, VFS RPCs including delete, frame-size caps, handshake/idle timeouts, and client/server version validation. Tests cover loopback, TLS handshake, auth failure, prepared statements, result typing, shutdown, session reuse, VFS operations, and installed server artifacts. | OpenSSL, `capdb_stream_*`, `capdb_proto`, network CMake option, server/client targets, CTest/CI smoke. |
| 7 | complete | 100% | Authentication and basic server hardening. Token auth, `user:password` auth, `token_file`, hashed `sha256:<hex>` token/password storage, constant-time comparison, auth throttling, TLS-by-default server startup, loopback-only insecure client mode unless explicitly widened, audit logging, docs, and regression coverage are implemented. | Network transport, auth file, OpenSSL SHA-256, TLS config, server session loop. |
| 8 | complete | 100% | Path jail and access-safety controls. Legacy `--db-root` and volume `--volume-root` paths are canonicalized, access is kept under configured roots, `ATTACH`/`DETACH` are denied where relevant, same-path pool/VFS conflicts are guarded, and remote VFS delete is routed through the same jail and path registry. Tests cover attach denial, open switching, VFS/pool exclusion, separate VFS paths, and guarded VFS delete. | Server path resolver, authorizer, pool/VFS path registry, filesystem `realpath`. |
| 9 | complete | 100% | Remote SQL client C API. `capdb_net_connect/exec/prepare/step/finalize/close`, typed column access, `changes`, `last_insert_rowid`, cancel, alive checks, URI parsing, TLS options, token/password/hash auth, remote VFS RPCs, VFS delete, and prepared-statement stream ownership are implemented, installed, documented, and exercised by C tests. Replica balancing remains the separate row-15 feature. | Network transport, protocol, TLS, server remote SQL implementation, VFS RPCs. |
| 10 | complete | 100% | Remote VFS (`capdbvfs`). Client-side pager with server-side page I/O supports open/read/write/truncate/sync/size/lock/check-reserved/delete and SQL over the VFS. `xDelete` now issues a server-side, jail-checked delete RPC that refuses busy pool/VFS paths, and CTest verifies server-side file removal. The VFS remains intentionally non-HA, which is a documented scope choice rather than an incomplete row-10 requirement. | Network client/server, VFS RPC messages, path registry, SQLite VFS layer, VFS delete regression. |
| 11 | complete | 100% | Server-side remote SQL execution. `capdb-server` accepts clients, authenticates plaintext or SHA-256 auth-file secrets, opens database pools, executes SQL, streams rows, enforces transaction contracts, blocks `EXEC` with pinned statements, caps large `EXEC` result sets, tracks changes/rowids, serves all VFS RPCs including delete, and runs concurrent sessions. The C suite covers the behavioral contracts. | Pool, protocol, auth, TLS, path jail, statement/session state, CTest. |
| 12 | complete | 100% | Volume store and `capdbstorevfs`. Volume directories, manifests/state, `data/main.db` sidecars, framed WAL segments, LSN tracking, generation fields, persisted role state, direct volume SQL, export, snapshot path reporting, and VFS registration exist and pass direct plus network volume tests. The remaining HA policy is tracked in replication/cluster rows rather than store completeness. | Store format, filesystem layout, core VFS integration, pool, replication hooks, snapshot tests. |
| 13 | complete | 100% | Primary/replica WAL replication. Primary sender, replica receiver, framed WAL chunks, token auth, async and sync modes, generation fencing, ordered replay, checkpoint-before-read, sync ACK waits, and receiver reconnect from local applied LSN are implemented. CTests cover async and sync primary-to-replica read visibility plus read-preference behavior over replicated data. | Volume store, replication protocol, TLS/frame transport, cluster generation state, server hooks. |
| 14 | complete | 100% | Cluster administration and failover. `capdb-ctl status/promote/demote/backup/export` builds, is installed, has a man page, and persists local role state. Promotion replays replication segments, waits up to `--wait MS` for applied LSN catch-up before generation bumping, marks role primary, and reports timeout as busy; demotion marks role demoted. External leader election/fencing remains an operator/orchestrator concern documented in ops guidance. | Volume store, replication recovery, generation metadata, persisted role state, admin CLI tooling. |
| 15 | complete | 100% | Replica read fan-out / read preference. URI parsing accepts `read_preference=replica` and comma-separated `replicas=...`; the client attempts each endpoint, keeps reachable replica streams, round-robins read-only `EXEC`/`PREPARE` across them, and falls back to primary if none are reachable. Prepared `STEP`/`FINALIZE` retain their selected stream, and regression coverage proves dead-first-replica failover plus primary-shutdown read survival. | Network client URI parser, `capdb_net_exec`, replica endpoint pool, replication consistency. |
| 16 | complete | 100% | Go driver. Network and embedded `database/sql` drivers exist under build tag `capdb`, support exec/query/transactions, typed values, context cancellation for network calls, embedded parameter binding, and conformance tests. Go tests are wired into CI and the release script ships the Go binding in `capdb-bindings-<ver>.tar.gz`. | `libcapdb`, `libcapdb_client`, CGO, env helper, Go tests, binding release bundle. |
| 17 | complete | 100% | Rust binding. The crate provides embedded and network modes, execution metadata, JSON query results, FFI bindings, and tests in CI. `capdb-rust-build-env.sh` exports the variables its usage promises, and the release script ships the Rust crate in `capdb-bindings-<ver>.tar.gz`. The JSON-oriented API is the supported Rust surface for this release. | `libcapdb.so`, Rust FFI, `build.rs`, serde_json, tempfile tests, binding release bundle. |
| 18 | complete | 100% | Python DB API binding. The binding works from repo root through a `capdb/` import shim, supports embedded smoke without manual `PYTHONPATH`, has an in-repo smoke test, CI smoke, working env helper exports, and ships in `capdb-bindings-<ver>.tar.gz`. | `libcapdb.so`, Python `ctypes`, DB API wrapper, repo-root import shim, binding release bundle. |
| 19 | complete | 100% | Java/JNI surface. `capdb/jni/capdb_jni.c` provides remote connect/close/exec JNI calls, `bindings/java/org/capdb/jni/wrapper1/Capdb.java` provides a minimal Java wrapper, CMake compiles the wrapper when `javac` is available, installs the loadable `libcapdb_jni.so` plus wrapper source when JNI headers exist, release packaging includes the Java binding source, and CTest verifies runtime library loading. | JNI headers/JDK, `libcapdb_client`, CMake `CAPDB_ENABLE_JNI_NETWORK`, Java wrapper compile/runtime tests. |
| 20 | complete | 100% | Packaging, install, and release artifacts. Install rules cover CLI, server, `capdb-ctl`, static/shared libs, generated headers, pool/client/store/cluster/replication headers, CLI/server/admin man pages, `capdb.pc`, `libcapdb_client.pc`, helper scripts, JNI artifacts when available, Java wrapper source, systemd template, CPack source/binary tarballs, amalgamation bundle, and versioned binding-source bundle. CI install smoke and release workflow exercise these paths. | CMake install rules, CPack, release script, pkg-config, generated sources, docs/man pages, GitHub workflows. |
| 21 | complete | 100% | Active native and binding test coverage. CTest now covers pool/network/store/replication contracts, extension smoke, JNI compile plus runtime library load, install-tree smoke, release artifact creation/audit, CapDB-native upstream-archive smoke, fuzz probes, soak probes, upstream-sync checks, and operations health/restore smoke. Warning build, codegen parity, API-surface audit, Go/Rust/Python binding checks, and CI smoke coverage are wired. Longer external chaos campaigns remain useful but are no longer a missing repo-level test harness. | CMake/CTest, GitHub Actions, binding toolchains, smoke scripts, release audit, fixtures. |
| 22 | complete | 100% | Documentation, man pages, and architecture docs. README/user/build/layout/ADR/store/pool/replication/release/upstream/operations docs, CLI/server/admin man pages, extension/JNI docs, release workflow, helper-script usage, install artifact tables, and HA/read-preference limits now match live code. `capdb-ctl(1)` documents prepare/status/health/metrics/promote/demote/backup/export, and build docs reflect the shared JNI library artifact. | Live source, tests, release state, docs maintenance, man pages. |
| 23 | complete | 100% | Production operations layer. Installed admin tooling now supports volume prepare, JSON status, JSON health/readiness, Prometheus-style metrics, backup/export, restore-drill validation, promote/demote, quiet-mode audit controls, and systemd packaging. The runbook documents service setup, health checks, metrics, structured audit logs, backup validation, restore drills, manual promotion, upgrade guardrails, and required external fencing. `capdb_ops_smoke` verifies the local operational path. | Stable server/replication behavior, admin CLI, packaging, observability scripts, runbook. |
| 24 | complete | 100% | Long-term upstream merge and compatibility strategy. Layout docs explain upstream path mapping and generated-source rules, `docs/UPSTREAM_SYNC.md` defines merge rules and verification, `tools/upstream-sync-check.sh` automates the quick/full sync gate, `docs/upstream/COMPATIBILITY.md` records the compatibility ledger/template, and CTest runs the quick upstream-sync check plus selected archive/fuzz/soak smoke. | Core fork policy, upstream SQLite releases, codegen parity checks, API-surface audit, compatibility ledger. |

## Recommended Completion Order

All tracked feature rows are now complete. Keep this matrix current as new product scope is added; future work should enter as new rows rather than reopening already completed implementation rows without a concrete regression or requirement change.
